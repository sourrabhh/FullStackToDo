spring:
  datasource:
    url: jdbc:mysql://${dbhost}:${dbport}/${dbschema}
    username: ${dbusername}
    password: ${dbpassword}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
